# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Pydantic
pydantic==2.10.4
pydantic-settings==2.7.0

# PDF extraction
PyPDF2==3.0.1
# REMOVED: pycryptodome — not used anywhere in the codebase

# Vector database
qdrant-client==1.12.0
# REMOVED: langchain-qdrant — you use qdrant-client directly (correct per constraints)

# Redis (async)
redis==5.2.1

# SQL database (async)
sqlalchemy==2.0.36
aiosqlite==0.20.0
greenlet==3.1.1

# LangChain (text splitters + LLM wrappers only — no RetrievalQAChain, no OpenAI)
langchain==0.3.14
langchain-core==0.3.0
langchain-google-genai==2.0.7
langchain-groq==0.2.0

# Embeddings
sentence-transformers==3.3.1
numpy==2.1.0
torch==2.6.0
transformers==4.47.0
huggingface-hub==0.27.0
tqdm==4.67.1

# Utilities
python-dotenv==1.0.1
